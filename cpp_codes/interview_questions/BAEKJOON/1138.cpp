#include <bits/stdc++.h>
using namespace std;

int main() {
    int n;
    cin >> n;
    int num_tallpeople[n];
    for (int i = 0; i < n; i++) {
        int input;
        cin >> input;
        num_tallpeople[i] = input;
    }

    // for (auto &elem : num_tallpeople) {
    //     cout << elem << " ";
    // } 
    int sequence[n];
    


}